import 'package:flutter/material.dart';

class AddChangesPractice extends StatelessWidget {
  const AddChangesPractice({super.key});

  @override
  Widget build(BuildContext context) {
    return Material(
      color: Colors.amber,
     child: Container(),
    );
  }
}